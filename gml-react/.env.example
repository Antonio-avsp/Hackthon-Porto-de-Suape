# Chave da API Anthropic para leitura de licenças por IA (OCR/visão).
# Copie este arquivo para ".env" e cole sua chave. A IA passa a funcionar
# automaticamente, sem nenhuma configuração na interface.
# Sem chave, o app opera em modo demonstração (extração simulada).
VITE_ANTHROPIC_API_KEY=
