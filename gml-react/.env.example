# URL do backend de IA (GML Backend). Padrão: http://localhost:3333
# Copie para `.env` para sobrescrever. Também pode ser ajustado em runtime
# pelo modal "⚙ Configurar IA" dentro do app.
VITE_API_URL=http://localhost:3333
